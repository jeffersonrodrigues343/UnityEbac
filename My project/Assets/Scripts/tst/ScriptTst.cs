/*using UnityEngine;

public class ScriptTst : MonoBehaviour
{


    public CapInt Capsule;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Capsule.ChangeColor();
        Capsule.MyTransform.position = new Vector3(1, 2, 3);
    }

}
*/